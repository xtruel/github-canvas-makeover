import 'dotenv/config';\nimport { z } from 'zod';\nconst envSchema = z.object({ PORT: z.coerce.number().default(4000), DATABASE_URL: z.string(), JWT_SECRET: z.string(), DEFAULT_LOCALE: z.string().default('it'), ENABLE_REGISTRATION: z.string().default('true'), ADMIN_EMAIL: z.string().email().optional(), ADMIN_PASSWORD: z.string().optional(), CORS_ORIGINS: z.string().default('http://localhost:5173'), MEDIA_BUCKET: z.string().optional(), MEDIA_REGION: z.string().optional(), MEDIA_ENDPOINT: z.string().optional(), MEDIA_ACCESS_KEY: z.string().optional(), MEDIA_SECRET_KEY: z.string().optional(), MEDIA_BASE_URL: z.string().optional(), FORCE_LOCAL_STORAGE: z.string().default('false'), LOG_LEVEL: z.string().default('info'), RATE_LIMIT_ENABLED: z.string().default('true') });\nexport const env = envSchema.parse(process.env);\nexport const parsedCorsOrigins = env.CORS_ORIGINS.split(',').map(o => o.trim());\nexport const isProd = process.env.NODE_ENV === 'production';\n
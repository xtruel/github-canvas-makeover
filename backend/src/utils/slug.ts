import crypto from 'crypto';\nexport function toSlug(title: string): string { const base = title.toLowerCase().replace(/[^a-z0-9\s-]/g, '').trim().replace(/\s+/g, '-').replace(/-+/g, '-'); return base || crypto.randomBytes(4).toString('hex'); }\nexport function uniqueSlug(base: string, collisionIndex: number): string { if (collisionIndex === 0) return base; return `${base}-${collisionIndex}`; }\n
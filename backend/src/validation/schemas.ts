import { z } from 'zod';\nexport const registerSchema = z.object({ email: z.string().email(), password: z.string().min(6) });\nexport const loginSchema = registerSchema;\nexport const articleCreateSchema = z.object({ title: z.string().min(3), body: z.string().min(1), language: z.string().optional(), status: z.enum(['DRAFT', 'PUBLISHED']).optional(), coverMediaId: z.string().optional() });\nexport const articleUpdateSchema = articleCreateSchema.partial();\nexport const mediaPresignSchema = z.object({ filename: z.string().min(1), mimeType: z.string().min(1), type: z.enum(['IMAGE', 'VIDEO']) });\n
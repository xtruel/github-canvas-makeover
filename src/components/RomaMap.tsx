import React from 'react';

/**
 * Stub temporaneo della mappa interattiva RomaMap.
 * La precedente implementazione è stata rimossa temporaneamente per effettuare un refactor/ottimizzazione.
 * TODO: Reintrodurre nuova versione ottimizzata della mappa (performance + UX migliorata).
 */
const RomaMap: React.FC = () => {
  return null;
};

export default RomaMap;
